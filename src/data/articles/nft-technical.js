export const nftTechnicalArticle = {
  slug: "how-internet-works-to-nfts-technical-guide",
  title: "From Internet Protocols to Web3: A Technical Explanation of NFTs",
  description: "A foundational guide explaining how the internet works, the evolution of web architecture, blockchain smart contracts, and the technical mechanics behind NFTs.",
  date: "2026-07-22",
  readTime: "9 min read",
  category: "Blockchain Technology",
  content: `
    <h2>1. The Foundation: How Does the Internet Work?</h2>
    <p>To understand modern Web3 technologies like NFTs, we first need to understand the underlying infrastructure of the internet. At its core, the internet is a globally connected network of computers that communicate using standardized rules called <strong>protocols</strong>.</p>
    <ul>
      <li><strong>IP (Internet Protocol):</strong> Gives every connected device a unique address (like a digital street address) so data packets know where to go.</li>
      <li><strong>TCP (Transmission Control Protocol):</strong> Breaks data down into small packets, sends them across various physical routes (undersea fiber-optic cables, routers), and reassembles them accurately at the destination.</li>
      <li><strong>HTTP / HTTPS:</strong> The protocol your web browser uses to request web pages from remote servers and render HTML, CSS, and JavaScript onto your screen.</li>
    </ul>

    <h2>2. The Evolution of the Web: Web1 to Web3</h2>
    <p>Over the decades, the way we use these network protocols has evolved through three distinct phases:</p>
    <ul>
      <li><strong>Web1 (Read-Only):</strong> The early internet (1990s–2000s). Static web pages hosted on simple servers where users could only consume content.</li>
      <li><strong>Web2 (Read-Write):</strong> The modern centralized web (2000s–Present). Users create content via centralized platforms (social networks, cloud databases). However, users do not truly own their data or digital assets—the platform corporations do.</li>
      <li><strong>Web3 (Read-Write-Own):</strong> A decentralized paradigm built on top of the internet that allows users to verify ownership of digital assets without relying on a central database administrator.</li>
    </ul>

    <h2>3. Enter Blockchain and Smart Contracts</h2>
    <p>A <strong>Blockchain</strong> is a distributed, peer-to-peer database replicated across thousands of computers (nodes) globally. Instead of a single company controlling the database, consensus algorithms (like Proof of Stake) ensure that nobody can alter past records fraudulently.</p>
    <p><strong>Smart Contracts</strong> are self-executing programs deployed directly onto the blockchain (such as Ethereum). Once deployed, they run automatically based on hardcoded conditions, removing the need for intermediaries.</p>

    <h2>4. What is an NFT Technically? (ERC-721 Standard)</h2>
    <p>An NFT (Non-Fungible Token) is essentially a specific smart contract deployed on a blockchain network that adheres to standard interfaces—most commonly the <strong>ERC-721</strong> or <strong>ERC-1155</strong> token standards.</p>
    
    <p>Technically, an NFT consists of three main components:</p>
    <ul>
      <li><strong>Token ID:</strong> A unique uint256 integer generated by the smart contract upon minting.</li>
      <li><strong>Owner Address:</strong> A cryptographic wallet address mapping directly to the Token ID inside the contract's public state memory.</li>
      <li><strong>Token URI (Metadata Pointer):</strong> A link (often pointing to IPFS - InterPlanetary File System) that contains a JSON object describing the asset (name, image URL, traits, and properties).</li>
    </ul>

    <h2>5. Why Decentralized Media Storage (IPFS) Matters</h2>
    <p>A common technical misconception is that the actual image or 3D model file is stored directly inside the blockchain token. Because storing megabytes of raw data on a blockchain is extremely expensive, NFTs store a cryptographic link to the media using <strong>IPFS</strong>.</p>
    <p>Unlike traditional web links (HTTP) that break if a server goes down, IPFS uses <i>content addressing</i> (hashes). This ensures that the metadata cannot be modified or replaced without invalidating the token's cryptographic signature.</p>
  `
};